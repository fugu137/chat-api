# suppress inspection "UnusedProperty" for whole file

spring.datasource.url=jdbc:postgresql://localhost:5432/${TEST_POSTGRES_DB}
spring.datasource.username=${TEST_POSTGRES_USER}
spring.datasource.password=${TEST_POSTGRES_PASSWORD}

cors.allowed.origins=http://allowed-url.com

logging.level.root=ERROR